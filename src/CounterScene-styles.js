// @flow
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 20,
    flex: 1,
    backgroundColor: '#f99',
    alignItems: 'center',
    justifyContent: 'center',
  },
  counterText: {
    color: 'black',
  },
});
